# Tasks: Home Tool Redesign

**Plan**: [plan.md](./plan.md)

> Format reference: `[P]` markers and parallel groups — see `skills/tasks/SKILL.md` § Phase rules.

## Phase 1: Core Implementation

- [x] **T001** [P] Replace position selector with settings row + trim footer links — `libs/ngx-dev-toolbar/src/tools/home-tool/home-tool.component.ts` | R001, R002, R005
  - **Do**: (a) Replace the `.position-selector` segmented bar block in the template with an `.instruction` row matching the Export / Import / Reset pattern: title "Toolbar Position" + helper text on the left, a **compact segmented control** inside `.instruction__control` on the right with six abbreviated keys (`top | bottom | left | right | ◧ sidebar-L | ◨ sidebar-R`), bound to `state.position()` via a click handler. **Fallback**: if the 6-key strip overflows the row at small/medium window widths, swap the `.instruction__control` content to `<ndt-select>` with the same `state.position()` binding. (b) Trim the `links` array to `[Docs, Bug report]` only — remove Suggestions, Star, Community.
  - **Verify**: `nx serve demo` → open Home Tool, position row reads as a settings row aligned with Export / Import / Reset; footer shows only 2 links sitting tight on the left; `nx lint ngx-dev-toolbar` clean; type check passes.
  - **Leverage**: existing `.instruction` rows (Export / Import / Reset) already in the same template — copy that structure; `state.position()` signal already drives the old segmented bar so the binding is a one-line move.

- [x] **T002** [P] Remove `.position-selector` styles + add Layout/Data divider + tighten footer pair — `libs/ngx-dev-toolbar/src/tools/home-tool/home-tool.component.scss` | R005, R007, R001
  - **Do**: (a) Delete the `.position-selector` block (~lines 93–131). (b) Add a quiet top border + `padding-top` on the *first* "Data" `.instruction` row (Export Settings) so a divider visually separates "Layout" from "Data" — mirror the `.instruction--danger-zone` divider treatment but quieter (e.g. `border-top: 1px solid var(--ndt-border-primary); padding-top: var(--ndt-spacing-md);`). (c) Change `.footer-links` from `justify-content: space-between` (line 142) to `justify-content: flex-start; gap: var(--ndt-spacing-md);` so the 2 remaining links read as a tight pair, not a stretched bar.
  - **Verify**: `nx run ngx-dev-toolbar:build` succeeds (SCSS budget warnings are pre-existing, not new errors); divider appears between the Position row and the Export row; footer links sit tight on the left in both light and dark themes.
  - **Leverage**: `.instruction--danger-zone` already implements a divider treatment in this same file — use it as the visual reference for the lighter "Data" divider.

- [x] **T003** [P] Add `tooltipDirection` mapping for sidebar positions — `libs/ngx-dev-toolbar/src/components/tool-button/tool-button.component.ts` | R003, NFR002
  - **Do**: (a) Add `readonly tooltipDirection = computed(() => { const p = this.position(); return p === 'sidebar-left' ? 'left' : p === 'sidebar-right' ? 'right' : p; });` (cardinal `top` / `bottom` / `left` / `right` pass through unchanged). (b) Change the host class binding from `'tooltip-position-' + position()` to `'tooltip-position-' + tooltipDirection()`. **No SCSS changes** — existing `:host(.tooltip-position-left)` / `:host(.tooltip-position-right)` rules already point the tooltip toward the viewport interior, which is exactly what sidebar mode needs.
  - **Verify**: `grep -rn "tooltip-position-" libs/ngx-dev-toolbar/src` confirms the class is consumed only in `tool-button.component.scss` (no silent regressions in other components); type check passes; serving the demo and switching to `sidebar-right` shows tooltips opening to the **left** of buttons (and `sidebar-left` opens to the **right**) without clipping at the viewport edge.
  - **Leverage**: the `position` input is already declared on this component; the existing host class binding is the only structural line that needs to change.

- [x] **T004** [P] Match close-button hover to siblings + verify default-mode header alignment — `libs/ngx-dev-toolbar/src/components/window/window.component.scss` | R004, R006, NFR003
  - **Do**: (a) **R004 — option A (neutral parity)**: delete the `.control--close:hover { background: var(--ndt-hover-danger); }` override so close inherits the sibling `.control:hover` neutral treatment (`var(--ndt-hover-bg)` + `var(--ndt-text-primary)`). *Fallback option B if neutral feels under-signalled in review:* replace the override with `background: rgba(var(--ndt-danger-rgb), 0.08); color: var(--ndt-danger);` for a soft, intentional close-only accent. (b) **R006 default-mode header pass**: inspect `.ndt-header { align-items: flex-start }` (line 38) in the demo across the six positions; if controls sit too high above a multi-line title, change default-mode to `align-items: center` and rely on the existing compact override (line 54) to keep compact mode centered too. May be a no-op edit — confirm visually before committing.
  - **Verify**: hover the close button in light + dark themes — hover background and color match the minimize/maximize hovers (no red flash); run a contrast checker on the resulting hover swatch to confirm WCAG AA in both themes (NFR003); header in default mode looks balanced against the title block; compact (sidebar) mode still centers controls against the title row.
  - **Leverage**: the sibling `.control:hover` rule in the same file already defines the neutral treatment to inherit — option A is just removing the override.

- [x] **T005** Add unit tests for sidebar tooltip direction mapping *(depends on T003)* — `libs/ngx-dev-toolbar/src/components/tool-button/tool-button.component.spec.ts` | R003, NFR002
  - **Do**: Extend (or create if missing) `tool-button.component.spec.ts` with Jest cases following the AAA pattern: (1) `position='sidebar-left'` → host classList contains `'tooltip-position-left'`; (2) `position='sidebar-right'` → host classList contains `'tooltip-position-right'`; (3) cardinal positions (`top`, `bottom`, `left`, `right`) pass through unchanged (one parameterized `it.each` block or four explicit cases).
  - **Verify**: `nx test ngx-dev-toolbar --testPathPattern=tool-button` — all new tests pass; full suite `nx test ngx-dev-toolbar` stays green.
  - **Leverage**: existing component test patterns under `libs/ngx-dev-toolbar/src/components/**/*.spec.ts` for `TestBed` / `ComponentFixture` setup with signal-based inputs.

## Phase 1.5: Post-CP1 Polish (added after screenshot review)

- [x] **T006** [P] Swap position segmented strip → `<ndt-select>` (documented fallback) — `libs/ngx-dev-toolbar/src/tools/home-tool/home-tool.component.ts` + `home-tool.component.scss` | R002, R005
  - **Why**: After T001 landed, the 6-key `.position-segmented` strip (~330–360 px natural width) consumed the right slot of the `.instruction` row, collapsing the label column to ~50 px. "Toolbar Position" wrapped to 2 lines and helper text stretched into a tall narrow column. Plan-022 already named `<ndt-select>` as the fallback for this overflow.
  - **Do**: (a) In `home-tool.component.ts`: import `ToolbarSelectComponent` from `../../components/select/select.component`, add to `imports` array. Rename `positionItems` → `positionOptions: SelectOption[]` (same `{ value, label }` entries). Replace the `<div class="position-segmented">…</div>` block with `<ndt-select [value]="state.position()" [options]="positionOptions" ariaLabel="Toolbar position" size="small" (valueChange)="onPositionChange($any($event))" />`. Keep `onPositionChange(position: ToolbarPosition)`. (b) In `home-tool.component.scss`: delete the `.position-segmented` block (~30 lines added in T002). Keep `.instruction--first-data` divider.
  - **Verify**: `npx tsc --noEmit -p libs/ngx-dev-toolbar/tsconfig.lib.json` clean; `nx lint ngx-dev-toolbar` clean; `nx serve demo` → Position row reads as a settings row with label + helper text fully on the left and a compact dropdown on the right; clicking the dropdown opens an overlay with all 6 options; selecting any option repositions the toolbar.
  - **Leverage**: existing `ndt-select` usages in i18n-tool, network-mocker-tool, feature-flags-tool. `SelectOption = { value: string; label: string }` is a drop-in for the `positionItems` shape.

- [x] **T007** [P] App Features rhythm fixes — `libs/ngx-dev-toolbar/src/components/list/list.component.scss` + `list-group/list-group.component.scss` | spacing audit
  - **Why**: Vertical rhythm at the App Features panel (and every other list-using tool) breaks at the boundaries: header→filter = 6px (`.tool-header` margin-bottom), filter→list = ~10px (gap inheritance + sibling collapse), group-header→items = 8px (host `gap: 4px` + `.content { margin-top: 4px }` stack), while internals are 4px. Two surgical CSS edits unify the boundaries.
  - **Do**: (a) In `list.component.scss`: add `padding-top: var(--ndt-spacing-sm);` to the `.list-container` block (around line 39, after `gap: var(--ndt-spacing-xs)`) so filter-row → first-group-header = 6px, matching header → filter row at 6px. (b) In `list-group.component.scss`: delete `margin-top: var(--ndt-spacing-xs);` from the `.content` block (line 82). The host's `gap: var(--ndt-spacing-xs)` (line 8) already provides 4px between header and content; the duplicate margin-top stacked it into 8px.
  - **Verify**: `nx serve demo` → Open App Features tool. header → filter = 6px, filter → first group = 6px, group header → first item = 4px (tighter, not airy). Open Feature Flags / Permissions / Presets tools — same fix applies; nothing should regress.
  - **Leverage**: shared layout primitives — single edits benefit all list-using tools.

- [x] **T008** [P] Footer rename: Bug report → Feedback — `libs/ngx-dev-toolbar/src/tools/home-tool/home-tool.component.ts` | R001
  - **Why**: User wants a single "Feedback" link consolidating bug reports + feature requests. Final list: `[Docs, Feedback]`.
  - **Do**: In `home-tool.component.ts` `links` array, replace `{ icon: 'bug', url: 'https://github.com/alfredoperez/ngx-dev-toolbar/issues', label: 'Bug report' }` with `{ icon: 'lightbulb', url: 'https://github.com/alfredoperez/ngx-dev-toolbar/issues/new/choose', label: 'Feedback' }`. The `/issues/new/choose` URL drops users on GitHub's issue-template picker so they can pick "Bug report" or "Feature request" when filing.
  - **Verify**: `nx serve demo` → footer shows `Docs · Feedback` with lightbulb icon; clicking Feedback opens GitHub's issue-template chooser.
  - **Leverage**: `lightbulb` icon already registered in `icon.models.ts` (was the original "Suggestions" icon before T001 trimmed the link).
