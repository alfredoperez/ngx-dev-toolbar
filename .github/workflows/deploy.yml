name: Deploy documentation to GitHub Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ['main']

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Build documentation
        run: npx nx run documentation:build --configuration=production
      - name: Build demo
        run: npx nx run ngx-dev-toolbar-demo:build --configuration=production
      - name: Combine builds
        run: |
          mkdir -p dist/apps/documentation/browser/demo
          cp -r dist/apps/ngx-dev-toolbar-demo/browser/* dist/apps/documentation/browser/demo/
      - name: Setup Pages
        uses: actions/configure-pages@v4
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'dist/apps/documentation/browser'
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
